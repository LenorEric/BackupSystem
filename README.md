# BackupSystem
Lenor

## Backup

在tasklist.json中设置好想要备份的文件。然后运行backup.py。

如果启用了“重新确认”，则每次备份前，需要删除tasklist.json中"Need Re-confirm"的内容（使其变为""）

## Restore

运行restore.py，然后选择对应的dirfile和想要恢复到的目录，程序会自动恢复。
